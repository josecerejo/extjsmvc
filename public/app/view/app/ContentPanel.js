Ext.define('ExtMVC.view.app.ContentPanel', {
  extend: 'Ext.Panel',
  alias: 'widget.ContentPanel',
  border: false,
  frame: false,
  autoHeight: true,
  initComponent: function() {

    var cmp = this;


    Ext.apply(this, {
    });

    this.callParent(arguments);
  }
});
